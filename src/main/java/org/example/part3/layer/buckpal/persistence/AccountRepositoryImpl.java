package org.example.part3.layer.buckpal.persistence;

import org.example.part3.layer.buckpal.domain.AccountRepository;

class AccountRepositoryImpl implements AccountRepository {
    //이렇게 pacakage-private으로 설정하면 의존성 역전을 지킬 수 있지요
}
